apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    # grafana/helm-charts (the org repo) no longer serves this chart at
    # this path - grafana-community/helm-charts is the current maintained
    # location (verified live before wiring this up).
    repoURL: https://github.com/grafana-community/helm-charts.git
    path: charts/grafana
    targetRevision: main
    helm:
      releaseName: grafana
      parameters:
        - name: fullnameOverride
          value: grafana
        # No adminPassword set here on purpose - the chart auto-generates
        # one into a Secret (name: "grafana", keys admin-user/admin-password)
        # rather than putting a plaintext password in git.
      values: |
        datasources:
          datasources.yaml:
            apiVersion: 1
            datasources:
              - name: Prometheus
                type: prometheus
                url: http://prometheus-server.monitoring.svc.cluster.local
                access: proxy
                isDefault: true

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  # Manual sync only - no automated/selfHeal. Run `argocd app sync grafana`
  # (or sync from the UI) to apply changes.
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
