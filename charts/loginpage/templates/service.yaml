apiVersion: v1
kind: Service
metadata:
  name: {{ include "loginpage.fullname" . }}
  labels:
    {{- include "loginpage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loginpage.selectorLabels" . | nindent 4 }}
